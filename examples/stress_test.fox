ambil "conv"

fungsi main() void
    # Loop to generate garbage
    var i = 0
    selama i < 500
        var s = "Garbage " + conv.Itoa(i)
        # Trigger allocation
        var a = [i, i + 1, i + 2]
        i = i + 1
    akhir

    # Check if swap works (Daemon should swap this out if we sleep)
    var retained = "I should be swapped"

    native_print("Waiting for Daemon GC...")

    # Access retained variable to trigger swap-in
    native_print(retained)

    native_print("Stress Test Done")
akhir
